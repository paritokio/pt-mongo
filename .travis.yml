sudo: required

language: ruby

services:
  - docker

before_install:
  - docker build -t paritokio-mongo .
  - docker run --name paritokio-mongo -d mongo
  - docker ps -a

script: 
  - bundle exec rake test

